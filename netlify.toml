[build]
  command = "npm run build"
  publish = "dist"

# The app uses hash-based routing, but keep a SPA fallback so any direct hit
# still serves index.html instead of a 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
